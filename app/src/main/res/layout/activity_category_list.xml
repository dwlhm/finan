<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- Header -->
    <com.dwlhm.finan.ui.common.ScreenHeaderView
        android:id="@+id/category_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:actionContentDescription="@string/category_add"
        app:actionIcon="@drawable/ic_add"
        app:titleText="@string/category_title" />

    <!-- Search Input Bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="46dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="10dp"
        android:background="@drawable/bg_control_surface"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <EditText
            android:id="@+id/category_search"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@null"
            android:hint="@string/category_search_hint"
            android:imeOptions="actionSearch"
            android:importantForAutofill="no"
            android:inputType="text"
            android:paddingStart="12dp"
            android:paddingEnd="8dp"
            android:textColor="@color/finan_text_primary"
            android:textColorHint="@color/finan_text_hint"
            android:textSize="14sp" />

        <ImageButton
            android:id="@+id/category_search_clear"
            android:layout_width="40dp"
            android:layout_height="match_parent"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/category_search_clear"
            android:src="@drawable/ic_close"
            android:visibility="gone" />
    </LinearLayout>

    <!-- Modern Segmented Filter Control Bar (Semua, Pengeluaran, Pemasukan) -->
    <com.google.android.material.button.MaterialButtonToggleGroup
        android:id="@+id/category_filter_toggle_group"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="12dp"
        app:checkedButton="@id/category_filter_all"
        app:selectionRequired="true"
        app:singleSelection="true">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/category_filter_all"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="42dp"
            android:layout_weight="1"
            android:text="Semua"
            android:textColor="@color/finan_text_primary"
            android:textSize="13sp"
            app:strokeColor="@color/finan_divider" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/category_filter_expense"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="42dp"
            android:layout_weight="1"
            android:text="Pengeluaran"
            android:textColor="@color/finan_text_primary"
            android:textSize="13sp"
            app:strokeColor="@color/finan_divider" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/category_filter_income"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="42dp"
            android:layout_weight="1"
            android:text="Pemasukan"
            android:textColor="@color/finan_text_primary"
            android:textSize="13sp"
            app:strokeColor="@color/finan_divider" />
    </com.google.android.material.button.MaterialButtonToggleGroup>

    <!-- Main List & States -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <ListView
            android:id="@+id/category_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:divider="@android:color/transparent"
            android:dividerHeight="8dp"
            android:paddingStart="16dp"
            android:paddingTop="4dp"
            android:paddingEnd="16dp"
            android:paddingBottom="108dp" />

        <ProgressBar
            android:id="@+id/category_loading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center" />

        <LinearLayout
            android:id="@+id/category_empty_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:orientation="vertical"
            android:paddingStart="24dp"
            android:paddingTop="24dp"
            android:paddingEnd="24dp"
            android:paddingBottom="80dp"
            android:visibility="gone">

            <TextView
                android:id="@+id/category_empty_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="@string/category_empty"
                android:textAppearance="@style/Finan.Text.Title" />

            <TextView
                android:id="@+id/category_empty_hint"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:gravity="center"
                android:text="@string/category_empty_hint"
                android:textAppearance="@style/Finan.Text.Body"
                android:textColor="@color/finan_text_secondary" />

            <Button
                android:id="@+id/category_empty_action"
                style="@style/Finan.Button.Primary"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="@string/category_empty_action" />
        </LinearLayout>
    </FrameLayout>
</LinearLayout>
